# ImageGallery
A simple PHP Image Gallery project that allows users to upload multiple images at once, store them securely on the server, and display them in a clean, responsive gallery. It demonstrates file handling, validation, and basic frontend layout for managing image collections efficiently
